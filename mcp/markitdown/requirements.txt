# NOTE: markitdown-mcp pins mcp~=1.8.0 -- installing it downgrades the SHARED mcp package for all servers in this environment (verified compatible: every other module pins mcp>=1.2).
markitdown-mcp
pytest>=8.0
